Modifies the configuration or metadata of an existing database. Typical uses include changing character sets, collations, read/write modes, or other database-wide properties without recreating it.

```mysql
ALTER DATABASE company_db SET READ_ONLY = OFF;
```