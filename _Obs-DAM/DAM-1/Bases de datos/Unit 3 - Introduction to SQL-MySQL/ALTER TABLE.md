Changes the structure of an existing table. This can include adding, modifying, or removing columns, changing data types, adding or removing constraints, and renaming elements, often without affecting existing data integrity.

```mysql
ALTER TABLE employees ADD salary DECIMAL(10,2);
```