Removes existing records. It can target specific rows based on conditions or remove all rows. Referential constraints may restrict or cascade deletions.

```mysql
DELETE FROM employees WHERE id = 1;
```