Adds new rows to a dataset. It can insert single or multiple records, accept explicit values, or derive values from a query. Constraints and defaults are enforced during insertion.

```mysql
INSERT INTO employees (id, name) VALUES (1, 'Alice');
```