Retrieves data from one or more sources. It supports projection, filtering, aggregation, joins, subqueries, and expressions, forming the core of data retrieval operations.

```sql
SELECT * FROM employees;
```