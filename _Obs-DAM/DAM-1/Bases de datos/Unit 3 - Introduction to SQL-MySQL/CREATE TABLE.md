Defines a new table structure by specifying columns, data types, constraints (such as primary keys, foreign keys, uniqueness, and nullability), and default values. It establishes how data will be stored and validated.

```sql
CREATE TABLE employees (
  id INT PRIMARY KEY,
  name VARCHAR(50)
);
```