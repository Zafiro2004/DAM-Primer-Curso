Modifies existing data by changing one or more values. It supports conditional updates, expressions, and subqueries, and must respect constraints and data integrity rules.

```mysql
UPDATE employees SET name = 'Bob' WHERE id = 1;
```