Defines the sort order of results based on one or more expressions, allowing ascending or descending ordering and controlling result presentation.

```mysql
SELECT * FROM employees 
ORDER BY name;
```