Permanently removes a table definition along with all stored data and associated metadata. This operation is irreversible and may cascade to dependent objects depending on configuration.

```mysql
DROP TABLE employees;
```