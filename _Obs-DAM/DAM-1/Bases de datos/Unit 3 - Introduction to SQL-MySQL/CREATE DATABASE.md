Creates a new logical container for storing data objects. It defines the database’s existence and may include configuration parameters such as character set, collation, storage options, and access rules depending on the SQL dialect.

```sql
CREATE DATABASE company_db;
```