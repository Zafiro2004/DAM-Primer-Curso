Creates an auxiliary data structure that improves data retrieval performance by allowing faster lookup and sorting. Indexes can be defined on one or multiple columns and may enforce uniqueness.

```mysql
CREATE INDEX idx_name ON employees(name);
```