Eliminates duplicate rows from the result set, returning only unique combinations of selected values. It operates after row selection and before ordering.

```mysql
SELECT DISTINCT name FROM employees;
```